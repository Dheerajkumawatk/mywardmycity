# /public/images

- `candidate-portrait.webp` — hero portrait. Currently an AI-generated
  placeholder: a transparent-background cutout (1199×1499, alpha WebP) of a
  candidate in a namaste pose, so the head breaks past the blue circle and the
  skyline shows behind. Replace with an approved campaign image in the same
  form — waist-up cutout on a transparent background, subject anchored to the
  bottom edge, roughly 4:5.
- `candidate-portrait.placeholder-woman.webp` — the previous opaque stock
  placeholder, kept for reference. Safe to delete.

Everything else on the page (logo emblem, section icons) is inline SVG in
`components/` — no image files required.
